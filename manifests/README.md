# Kubernetes Manifests

This folder is designed for raw Kubernetes YAML files to define resources like Deployments, Services, ConfigMaps, etc., without using Helm.

## Use
Add `.yaml` files here directly to define Kubernetes resources for your application.

Placeholder content for Kubernetes manifests.